-- CodeLens schema baseline.
-- Domain tables (User, Assignment, Submission, ReviewPairing, Review, Grade, …)
-- will be added in later Flyway migrations as features are implemented.
--
-- Intentionally empty of tables so the API can start against a fresh database.
SELECT 1;
